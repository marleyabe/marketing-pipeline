name: ads2u
version: 1.0.0
profile: ads2u

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ads2u:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
